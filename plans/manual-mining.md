Players should be able to use a pick ax, or similar, as a way to get ores in the early game.
The resources they get will be the same from the miner, so should use the `Structure::mine` method.

Mining does not destroy the block being mined, just like miners.
