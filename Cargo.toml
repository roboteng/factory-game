[package]
name = "factory-game"
version = "0.1.0"
edition = "2024"

[features]
default = ["invariant-ckeck"]
invariant-ckeck = []

[dependencies]
bevy = "0.17.3"

[dev-dependencies]
pretty_assertions = "1.4.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
