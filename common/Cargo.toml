[package]
name = "common"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy.workspace = true
rand.workspace = true
noise = "0.9.0"

[dev-dependencies]
pretty_assertions = "1.4.1"
tracing-subscriber = {version = "0.3", features = ["env-filter"]}
